# ============================================================================
# 项目依赖（统一）
# 适用于：本地启动 + CI 打包
# 要求：Python 3.11+
#
# 【重要】
# 本地启动（start.bat）也需要装这套依赖
# Python 3.11+ 才能保证所有 API 兼容（pandas 2.x、fastapi 0.110）
# ============================================================================
pyinstaller>=5.13,<7.0

# 框架
fastapi==0.110.0
uvicorn[standard]==0.27.1
python-multipart==0.0.9

# 数据处理
pandas==2.2.1
openpyxl==3.1.2
et_xmlfile>=1.0.0

# 验证依赖
pydantic==2.6.1
